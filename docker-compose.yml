version: "3.7"
services:
    node:
        build:
            context: react
            dockerfile: Dockerfile.dev
        ports:
            - "3000:3000"
        volumes:
            - ./react/src:/app/src:ro
        env_file:
            - ./react/.env
